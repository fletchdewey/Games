# Games

Browser games deployed to Cloudflare Workers, live at fletcherdewey.com.

## Build & Deploy
- `./build.sh` — assembles `public/falo3d.html` from `src/falo3d/*` and builds `index.html`
- `./deploy.sh` — builds, then `wrangler deploy`
